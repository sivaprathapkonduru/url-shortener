{
  "name": "enterprise-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:backend": "cd url-shortener-service && npm run lint",
    "format:backend": "cd url-shortener-service && npm run format",
    "type-check:backend": "cd url-shortener-service && npx tsc --noEmit",
    "test:backend": "cd url-shortener-service && npm run test",
    "test": "npm run test:backend",
    "test:integration": "cd url-shortener-service && npm run test:integration",
    "test:e2e": "cd url-shortener-service && npm run test:e2e"
  },
  "lint-staged": {
    "url-shortener-service/**/*.{ts,js}": [
      "npm run lint:backend",
      "npm run format:backend"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.0.0"
  }
}
